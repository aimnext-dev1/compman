[project]
name = "compman"
version = "0.1.0"
description = "Docker Compose stack management CLI"
requires-python = ">=3.10"
dependencies = ["click", "pyyaml"]

[project.scripts]
compman = "compman.cli:cli"

[tool.uv]
package = true
